# Telegram Course Bot - بوت بيع الكورسات

بوت تلغرام لبيع الكورسات بنظام الحلقات الفردية مع نظام دفع يدوي ومشاهدة آمنة للفيديوهات.

## المميزات

- 🔐 نظام مستويات (أدمن / مستخدم)
- 📚 تصفح الكورسات والحلقات عبر أزرار Inline
- 💳 نظام شراء يدوي (رفع إيصال + موافقة الأدمن)
- 💾 قاعدة بيانات SQLite
- 🔒 مشاهدة آمنة عبر Telegram Web App
- 📱 منع التشغيل على Desktop/Web (Mobile فقط)
- 🔑 نظام توكنات لروابط الفيديو

## متطلبات التشغيل

- Python 3.8 أو أحدث
- حساب بوت تلغرام (Bot Token)
- معرف المسؤول (Admin ID)

## التثبيت

### 1. تثبيت المكتبات المطلوبة

```bash
pip install -r requirements.txt
```

### 2. إعداد ملف البيئة

ملف `.env` موجود بالفعل ويحتوي على:
```env
BOT_TOKEN=8545413891:AAGo03_2tW9A8oIPz7CsVp0TEQdWbhly5Yw
ADMIN_ID=8420689565
WEBAPP_URL=http://localhost:5000
WEBAPP_HOST=0.0.0.0
WEBAPP_PORT=5000
TOKEN_EXPIRY_HOURS=24
```

⚠️ **مهم**: في الإنتاج، يجب تغيير `WEBAPP_URL` إلى رابط السيرفر الحقيقي (مثل: `https://yourdomain.com`)

### 3. تشغيل البوت

في نافذة Terminal الأولى:
```bash
python bot.py
```

في نافذة Terminal الثانية:
```bash
python webapp/server.py
```

## الاستخدام

### للمستخدمين

1. ابدأ محادثة مع البوت: `/start`
2. تصفح الكورسات المتاحة
3. اختر حلقة للشراء
4. ارفع صورة إيصال الدفع
5. انتظر موافقة الأدمن
6. شاهد الحلقة من قسم "مشترياتي"

### للمسؤولين

استخدم الأمر `/admin` للوصول إلى لوحة التحكم:

#### إضافة كورس جديد
1. اختر "إدارة الكورسات"
2. اضغط "إضافة كورس جديد"
3. أدخل العنوان والوصف والسعر

#### إضافة حلقة
1. اختر الكورس من قائمة الكورسات
2. اضغط "إضافة حلقة جديدة"
3. أدخل:
   - رقم الحلقة
   - العنوان
   - الوصف
   - السعر
   - ملف الفيديو

#### مراجعة طلبات الشراء
1. اختر "طلبات الشراء المعلقة"
2. اختر الطلب للمراجعة
3. اضغط "قبول" أو "رفض"

## هيكل المشروع

```
ahmed_bot/
├── bot.py                 # الملف الرئيسي للبوت
├── config.py             # إعدادات البوت
├── database.py           # معالج قاعدة البيانات
├── requirements.txt      # المكتبات المطلوبة
├── .env                  # متغيرات البيئة
├── handlers/
│   ├── user.py          # معالجات المستخدمين
│   ├── admin.py         # معالجات المسؤولين
│   └── payment.py       # معالجات الدفع
├── keyboards/
│   ├── user_kb.py       # أزرار المستخدمين
│   └── admin_kb.py      # أزرار المسؤولين
├── webapp/
│   ├── index.html       # صفحة مشغل الفيديو
│   ├── server.py        # خادم Flask
│   └── static/
│       └── style.css    # تنسيقات الصفحة
├── videos/              # مجلد الفيديوهات (اختياري)
└── data/
    └── bot.db          # قاعدة البيانات (يتم إنشاؤها تلقائياً)
```

## قاعدة البيانات

يتم إنشاء قاعدة البيانات تلقائياً عند التشغيل الأول. الجداول:

- `users` - معلومات المستخدمين
- `courses` - الكورسات
- `episodes` - الحلقات
- `purchases` - عمليات الشراء
- `video_tokens` - توكنات الوصول للفيديوهات

## نظام الأمان

### منع التشغيل على Desktop/Web

يتم فحص نوع الجهاز على مستويين:

1. **Client-side (JavaScript)**: فحص User Agent والمنصة
2. **Server-side (Flask)**: فحص إضافي على السيرفر

الأجهزة المسموحة:
- ✅ تطبيق تلغرام على Android
- ✅ تطبيق تلغرام على iPhone/iPad

الأجهزة الممنوعة:
- ❌ Telegram Desktop
- ❌ متصفحات الويب
- ❌ أجهزة الكمبيوتر

### نظام التوكنات

- كل رابط فيديو يحتوي على توكن فريد
- التوكن صالح لمدة 24 ساعة (قابل للتعديل)
- لا يمكن مشاركة الروابط (مرتبطة بالمستخدم)

## ملاحظات مهمة

### تخزين الفيديوهات

حالياً، البوت يدعم طريقتين:

1. **Telegram File IDs**: عند رفع الفيديو للبوت، يتم حفظ `file_id`
2. **ملفات محلية**: يمكن وضع الفيديوهات في مجلد `videos/`

⚠️ **للإنتاج**: يُنصح بتحميل الفيديوهات من Telegram Bot API أو استخدام خدمة تخزين سحابية.

### نشر البوت

لنشر البوت على سيرفر حقيقي:

1. استخدم خدمة استضافة (مثل: DigitalOcean, AWS, Heroku)
2. احصل على دومين ورابط HTTPS
3. غيّر `WEBAPP_URL` في `.env` إلى الرابط الحقيقي
4. استخدم `gunicorn` أو `nginx` لتشغيل Flask
5. استخدم `systemd` أو `supervisor` لتشغيل البوت

### الأمان الإضافي

للحماية القصوى، يمكنك إضافة:

- نظام DRM للفيديوهات
- Watermarking (علامة مائية)
- تشفير الفيديوهات
- تتبع محاولات التحميل

## الدعم الفني

للمشاكل والأسئلة:
- تحقق من ملفات الـ logs
- تأكد من تشغيل كل من البوت والـ Web App
- تأكد من صحة الـ Bot Token و Admin ID

## الترخيص

هذا المشروع مفتوح المصدر للاستخدام الشخصي والتعليمي.
